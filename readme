Основная цель: переместить файл с локального репозитория на удалённым(гитхаб)
Вторая цель: С удалённого на локальный
Для этого нужно открыть папку, в которой у вас файл с расширением .sln
Нажимаем ПКМ и выбираем Git Bash here
В эту консоль пишем git init(Создаёт папку гит)
После чего пишем git status 
После чего git add . ТОчка нужна, чтоб загрузить все файлы, вместо точки можно в ковычках указат имя одного файла, который хотите добавить 
git commit -m "init" 
Добавлены результаты по запросу «коммит это».
COMMIT (с англ. — «совершать, фиксировать») — оператор управления транзакциями языка SQL для успешного завершения транзакции. При выполнении оператора изменения, сделанные от начала транзакции и ранее не видимые для других транзакций, фиксируются в базе данных. Для отката всех изменений транзакции применяется оператор ROLLBACK. Эти две инструкции явным образом завершают транзакцию.
